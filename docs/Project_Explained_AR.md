# 📘 شرح مشروع SentinelAI بالتفصيل الكامل — من البيانات إلى القرار
*(نسخة القراءة الدائمة — نفس الشرح المقدم في المحادثة)*

---

## القصة الكبرى: لماذا هذا المشروع أصلاً؟

فرق الأمن السيبراني (SOC) تغرق يومياً بآلاف الأحداث من **ثلاثة عوالم مختلفة**:
تدفقات شبكة + رسائل نصية/بريدية + ملفات مشبوهة. الأدوات التقليدية تعتمد
"تواقيع" (Signatures) للمعروف فقط، فتفلت منها الهجمات الجديدة (Zero-Day)،
والفرز اليدوي بطيء، والأخطر: **قرار الاستجابة** (مراقبة؟ تنبيه؟ حجب؟ عزل؟)
يُتخذ يدوياً تحت الضغط — وكل خطأ له ثمن: التمرير = اختراق، والحجب الزائد =
تعطيل أعمال.

**SentinelAI** = منصة واحدة تحل المشكلة كاملة: تكشف، تسمّي، تُقيّم الخطورة،
**تقرر**، وتكتب التقرير — تلقائياً.

---

## 1) الوقود: البيانات (4 مصادر)

| المصدر | نوعه | حجمه | لماذا؟ |
|---|---|---|---|
| تدفقات الشبكة | مولّد موثّق | 25,200 سجل × 16 سمة × 5 فئات | المسموح في المتطلبات؛ موثق التوزيعات سطراً سطراً |
| رسائل SMS Spam | **حقيقي (UCI)** | 5,572 رسالة (747 سبام = 13.4%) | NLP على بيانات واقعية |
| صور البرمجيات | مولّد بأسلوب Nataraj 2011 | 720 صورة 48×48 × 4 عائلات | نهج "malware imaging" الأكاديمي |
| قائمة نطاقات خبيزة | **حقيقية تُنزَّل حياً** | 18,146 نطاقاً (blackbook) | برمجة ويب + استخبارات تهديد |

### أسرار توليد التدفقات (نقطة قوة للشرح):
- **التوزيع الطبقي مقصود**: Benign 45%، DDoS 20%، PortScan 15%، BruteForce 12%، Botnet 8% — لأن الحياة الحقيقية غير متوازنة.
- **فئة Benign خليط متعمد** من 5 سلوكيات: تصفح عادي 62% + **جلسات SSH إدارية 9%** + **تطبيقات beacon 8%** + **فحوص مراقبة 7%** + **ازدحام التحميل 14%** — هذه السلوكيات السليمة **تشبه الهجمات عمداً** (SSH يشبه BruteForce، والإشعارات الدورية تشبه Botnet C2) حتى توجد أخطاء واقعية تُناقش بدل دقة 100% مزيفة.
- **حقنّا عيوباً واقعية**: 0.8% صفوف مكررة، 1.5% قيم مفقودة في عمودين، 0.3% قيم مستحيلة (حجم حزمة سالب) — لتكون للمعالجة المسبقة قصة حقيقية.

### السمات الـ16 (افهم معانيها — سؤال متوقع):
`duration` مدة الاتصال، `protocol` TCP/UDP/ICMP، `src/dst_port` المنافذ،
`src/dst_bytes` حجم البيانات بالبايت، `src/dst_pkts` عدد الحزم،
`syn/ack/psh_rate` نسب أعلام TCP (قلب كشف DDoS!)، `avg_pkt_size` متوسط
حجم الحزمة، `byte_std` تشتت المحتوى، `flow_iat_mean` متوسط الزمن بين
التدفقات (قلب كشف الـ beaconing!)، `active_duration` مدة النشاط، `is_land`.

### بصمة كل هجوم (لماذا يستطيع النموذج التمييز؟):
- **DDoS**: syn_rate≈1 (عاصفة SYN)، آلاف الحزم الصغيرة بلا رد.
- **PortScan**: تدفقات فارغة فائقة القصر على منافذ عشوائية.
- **BruteForce**: محاولات دخول متكررة على منافذ 22/3389/21.
- **Botnet**: beaconing — حزم صغيرة منتظمة بفواصل طويلة (flow_iat_mean ضخم) على منافذ C2 كلاسيكية.

---

## 2) المطبخ: المعالجة المسبقة (كل خطوة بسببها)

| العملية | لماذا؟ (احفظ الصياغة) |
|---|---|
| حذف 200 مكرر | السجل المصدَّر مرتين يحرف التدريب ويضخم الدقة |
| الوسيط للمفقود | مقاوم للذيل الأيمن الثقيل بعكس المتوسط |
| قص السوالب للصفر | خلل حساسات لا وجود له فيزيائياً |
| One-Hot للبروتوكول | سمة اسمية بلا ترتيب — الترقيم المباشر يخترع ترتيباً وهمياً |
| log1p لـ6 أعمدة منحرفة | يضغط الذيل الأيمن ويقرب للطبيعي |
| StandardScaler على train فقط | KNN/SVM/MLP تحتاج مقاييس موحدة، وfit على الاختبار = تسريب (leakage) |
| SelectKBest (mutual info) → 12 من 17 | يزيح الضوضاء ويسرّع SVM |
| تقسيم طبقي 80/20 (~20k/5k) | يحفظ نسب الفئات في القسمين |

---

## 3) الحكم الأول: التعلم الخاضع للإشراف (6 نماذج + تحقق)

| النموذج | الدقة | لماذا هذا الأداء؟ |
|---|---|---|
| **RandomForest** 🥇 | **99.76%** | 200 شجرة تصوّت — تقليل التذبذب + التقاط اللاخطية والتفاعلات |
| DecisionTree | 99.2% | لا خطي لكنه وحده متذبذب (variance عالٍ) |
| SVM (RBF) | 97.5% | لا خطي؛ دُرّب على 10k لأن O(n²) — قرار موثق |
| KNN | 97.1% | قرب جغرافي؛ تأثر بالتداخل |
| LogisticRegression | 93.4% | خطي فقط — والفئات متداخلة فعلاً |
| NaiveBayes | 92.0% | افتراض استقلال السمات خاطئ هنا |

**التحقق من عدم الحظ (أهم ما يميز مشروعك):**
- 5-Fold CV: RF = 99.69% ± 0.0007 → النموذج مستقر على 5 تقسيمات مختلفة.
- GridSearchCV: أفضل إعداد = 200 شجرة بعمق 22.
- تجربة `class_weight='balanced'`: فرق شبه معدوم → الأخطاء سببها التداخل الحقيقي وليس عدم التوازن.

**قراءة مصفوفة الالتباس (بلا خوف):**
- DDoS وPortScan: صفر أخطاء — بصمتهما إحصائية حادة.
- الأخطاء تتركز: Benign↔BruteForce (SSH على 22!) وBenign↔Botnet (الإشعارات الدورية).
- **FP** (سليم→هجوم) = وقت محلل ضائع. **FN** (هجوم→سليم) = اختراق. لذلك Recall أغلى.

---

## 4) الحكم الثاني: غير الخاضع للإشراف (بلا labels إطلاقاً)

| التقنية | النتيجة | ماذا أثبتت؟ |
|---|---|---|
| K-Means (k=4 بـsilhouette) | purity 76.7%، ARI 0.46 | استعاد بنية الهجمات **بدون أي أlabels** |
| Hierarchical (Ward + Dendrogram) | ARI 0.47 | خوارزمية مختلفة تماماً وصلت لنفس النتيجة → البنية حقيقية |
| DBSCAN (على PCA-3D) | عناقيد + ضوضاء | منظور الكثافة |
| PCA | تصوير + مدخل DBSCAN | البصرة |
| **IsolationForest** (درّب على Benign فقط!) | precision 94.8%، recall 61.4%، إنذار خاطئ 4.2% | **كاشف الـ Zero-Day**: تعلّم "شكل الطبيعي" ورصد أي انحراف |

**الجملة الذهبية**: الخاضع يجيب "أي هجوم معروف هذا؟" — غير الخاضع يجيب
"هل هذا السلوك غريب أصلاً؟". الوكيل يستخدم الاثنين معاً.

---

## 5) التعلم العميق (3 معماريات — كل واحدة لنوع بياناتها الصحيح)

| المعمارية | البيانات | النتيجة | الدرس |
|---|---|---|---|
| **MLP** 12→64→32→5 | جدولية | 98.5% | أقل من RF (99.8%) — **صادق ومتوقع**: التجمعات الشجرية أقوى على الجدولي الصغير |
| **LSTM** Embedding→LSTM→Dense | نصوص | F1 0.944 > TF-IDF 0.918 | **ترتيب الكلمات** يضيف معلومة ("claim your prize" ≠ "prize your claim") |
| **CNN** Conv32→Conv64→Dense128 | صور | 100% | الالتفاف يقرأ الأنماط المكانية المحلية لعائلة البرمجية |
| HOG+SVM (خط أساس تقليدي) | صور | 100% | كلاهما يشبع المهمة هنا — CNN يتقدم على البيانات الحقيقية الأصعب |

---

## 6) التعلم المعزز: من "يعرف" إلى "يقرر"

**المشكلة**: بعد الكشف، من يختار الاستجابة؟ هذا **قرار** لا تصنيف.

- **الحالات (18)**: 3 تهديد (سليم/مشبوه/خبيث) × 3 ثقة × 2 حرجية الأصل.
- **الأفعال (4)**: Monitor / Alert_Analyst / Block_IP / Isolate_Host.
- **المكافأة = نموذج تكلفة SOC**: تفويت خبيث = −8، حجب سليم = −3، عزل سليم = −5، تصعيد صحيح = +2..+5.
- **قاعدة التعلم** (مكتوبة يدوياً بلا مكتبة RL):
  `Q[s,a] ← Q[s,a] + α·(r + γ·max Q[s',a'] − Q[s,a])`
- **الاستكشاف**: ε ينحدر 1.0→0.05.
- **النتيجة**: السياسة المتعلمة **+2.33** مقابل AlwaysBlock +1.0 وAlwaysMonitor **−3.67** والعشوائي −0.45.
- **السياسة تصاعدية منطقية** (انظر rl_policy.csv): سليم→Monitor، مشبوه→Alert/Block، خبيث حرج→Isolate (أعلى Q في الجدول: 23.9).

---

## 7) العقل المدبر: SentinelAgent (المرحلة 11)

سير العمل (احفظه بالترتيب):
```
استقبال الحدث
→ توجيه تلقائي: تدفق؟ رسالة؟ صورة؟ (يختار الأداة بنفسه!)
→ تنبؤ النموذج المناسب + درجة الثقة
→ رأي ثانٍ من IsolationForest (شذوذ؟)
→ إثراء استخباراتي: فحص IOC ضد 18,146 نطاقاً أسود
→ دمج درجة خطورة 0-100
→ قرار الاستجابة من جدول Q المتعلم
→ كتابة incident + التقرير الأمني + التوصيات
```
**لماذا Agent حقيقي؟** لأنه يُدرك، **يختار الأدوات**، يفسّر، يقرر ذاتياً، ويتصرف.

**القصة الذهبية (حدث 23)**: رسالة حكم عليها NLP بـ"ham" (سليمة) بثقة 0.69،
لكن الوكيل وجد نطاق `2020bill.com` في القائمة السوداء الحقيقية → رفع
الخطر +30 → **Isolate_Host**. الدرس: الدفاع الطبقي يمسك ما يفلت من النموذج.

---

## 8) الأرقام الذهبية (راجعها قبل النوم)

| البند | الرقم |
|---|---|
| سجلات التدفقات | 25,200 (بعد الحقن) / 25,000 نظيفة |
| رسائل حقيقية | 5,572 (13.4% سبام) |
| نطاقات سوداء حية | 18,146 |
| أفضل نموذج | RandomForest 99.76% / F1 0.997 |
| CV | 99.69% ± 0.0007 (5 folds) |
| MLP | 98.5% |
| LSTM | F1 0.944 (أفضل من TF-IDF 0.918) |
| CNN | 100% على 144 صورة اختبار |
| K-Means | purity 76.7% / k=4 |
| IsolationForest | precision 94.8% / recall 61.4% / FP 4.2% |
| RL | +2.33 ضد −3.67 (AlwaysMonitor) |
| الوكيل | 27 حدثاً × 3 محركات، دقة تدفقات 100% |
| الاختبارات | 17/17 |
| زمن التشغيل | ~159 ثانية على جهازك |

---

## 9) فلسفة المشروع (القسم 11 من ملف المتطلبات — تحقق حرفي)

```
Real Problem → Data Collection → Preprocessing → Supervised ML
→ Unsupervised ML → Evaluation → Advanced AI (DL+NLP+CV)
→ Decision (RL+Agent) → Visualization & Report → Presentation
```
كل سهم في هذه السلسلة له تنفيذ فعلي في المشروع. **هذا** هو Level 3 Expert.
